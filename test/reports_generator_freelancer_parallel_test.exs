defmodule ReportsGeneratorFreelancerParallelTest do
  use ExUnit.Case
  doctest ReportsGeneratorFreelancerParallel

  test "greets the world" do
    assert ReportsGeneratorFreelancerParallel.hello() == :world
  end
end
